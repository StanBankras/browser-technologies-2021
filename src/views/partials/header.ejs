<header>
  <div class="container">
    <nav>
      <ul>
        <li>
          <% if (userId) { %>
          <a href="/albums?userId=<%= userId %>">Albums</a>
          <% } else { %>
          <a href="/">Logout</a>
          <% } %>
        </li>
      </ul>
    </nav>
  </div>
</header>
<main>
  <div class="container">